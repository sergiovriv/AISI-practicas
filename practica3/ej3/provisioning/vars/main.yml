student_name: "Sergio Vila Riveira"
student_prefix: "svr2425"
webapp_container: "{{ student_prefix }}-webapp"
db_container: "{{ student_prefix }}-db"
webapp_img: "{{ student_prefix }}-web_img"
db_img: "{{ student_prefix }}-db_img"
nginx_container: "{{ student_prefix }}-nginx"
nginx_img: "{{ student_prefix }}-nginx_img"
container_network: "{{ student_prefix }}-network"
